import {
	commands,
	StatusBarAlignment,
	TextDocument,
	Uri,
	WebviewPanel,
	window,
	workspace,
} from "vscode";


const createWebview = (
  webviewPanel: WebviewPanel,
  document:{ kind: "text"; document: TextDocument }
  // options: DrawioClientOptions
)=>{};
